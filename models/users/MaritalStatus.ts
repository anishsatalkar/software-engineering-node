/**
 * @file Declares MaritalStatus enum that represents the marital status of a user.
 */

/**
 * @typedef MaritalStatus Represents the marital status of a user.
 */
enum MaritalStatus {
    Married = "MARRIED",
    Single = "SINGLE",
    Widowed = "WIDOWED"
}

export default MaritalStatus;